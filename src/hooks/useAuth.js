// Simulated useAuth hook (you should replace this with your actual authentication logic)
const useAuth = () => {
  // This is a placeholder. In a real app, this would come from your auth system
  const user = {
    type: "employer",
  };

  return { user };
};

export default useAuth;
